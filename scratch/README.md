# scratch/

**Deprecated.** One-off debug scripts were removed (v3.4.2).

Use supported tools instead:

- `scripts/diag_live_state.py` — parity report
- `scripts/run_startup_heal.py` — startup heal
- `scripts/repair_phantom_ledger.py` — phantom ledger purge

This folder may be deleted entirely if empty.
